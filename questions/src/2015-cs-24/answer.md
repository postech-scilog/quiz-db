NP의 정의에는 두가지가 존재합니다.

1. *non-determistic turing machine 이 다항시간 안에 해결 가능한 문제*: 이 정의로부터 NP라는 이름이
   붙었습니다.
2. *determinstic turing machine 이 다항시간 안에 (입력, 출력) 쌍의 올바름을 다항시간 안에 검증
   가능한 문제*: NP에 대해서 찾아보면 대부분 해당 정의를 사용합니다. 이는 non-deterministic turing
   machine은 이론적으로만 존재하는 계산 모델이기 때문입니다.