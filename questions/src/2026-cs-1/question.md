C 언어의 printf 함수는 첫 번째 인자인 format string을 해석하여 이후의 인자를 읽는다. 다음 코드에 대한 설명으로 올바른 것은?

```c
int x = 0;
printf("%d %d %d", x);
```

1. 컴파일 오류가 발생한다.
2. `0 0 0` 이 출력된다.
3. printf가 인자의 개수를 검사하여 런타임 오류를 보고한다.
4. 정의되지 않은 동작이 발생한다.
5. 운영체제가 함수 호출을 즉각 중단한다.